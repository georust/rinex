use crate::context::QcContext;

impl QcContext {
    /// Verification routine (will not panic, it only generates debug traces).
    /// From user input, it will help improve the provided context for next run.
    /// Input
    /// - ppp: set true if target is PPP solutions
    fn verification_debug_traces(&self, nav: bool, nav_ppp: bool) {
        // if observation
        //   if clock rinex: verify synchronicity

        
        
    }
}