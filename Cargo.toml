[workspace]
resolver = "2"

members = [
    "binex", 
    "rinex", 
    "rinex-cli", 
    "sp3",
    "sinex",
]

exclude = [
    "./test_resources",
]

[profile.release]
strip = true
opt-level = 3
lto = "thin"
codegen-units = 1
